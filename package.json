{
  "name": "kaoyan-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "考研助手 - 前端(client) + 后端(server) 工作区",
  "scripts": {
    "dev:client": "cd client && npm run dev:mp-weixin",
    "build:client": "cd client && npm run build:mp-weixin",
    "dev:server": "cd server && node app.js",
    "install:all": "cd client && npm install && cd ../server && npm install"
  }
}